spring:
  h2.console.enabled: true

  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:

  jpa.database-platform: org.hibernate.dialect.H2Dialect

  liquibase:
    change-log: classpath:config/liquibase/db-changelog-devmock.xml